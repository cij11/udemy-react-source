// Rules of reducers
// Must return ANY value besides 'undefined' (even null is acceptable)
// Produces 'state', or data to be used inside of your app,
// using only previous state and the action (reducers are pure)
// Must not return reach 'out of itself' to decide what value to return
// Must not mutate its input 'state' argument

// Reducer arguments: (state = initialState, action)

export default () => {}
